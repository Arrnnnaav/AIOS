[pytest]
markers =
    interactive: requires a normal interactive Windows desktop or real Win32/UIA windows
    pixel: captures and validates rendered desktop pixels
    hung: intentionally creates a non-responsive window and must run alone
